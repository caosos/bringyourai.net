# Required for the live Aria backend prototype.
OPENAI_API_KEY=sk-your-openai-api-key

# Optional. Defaults to gpt-4.1-mini.
ARIA_OPENAI_MODEL=gpt-4.1-mini

# Optional local server port. Defaults to 3000.
PORT=3000
